naftiko: 1.0.0-alpha2
info:
  label: Oracle Primavera P6 EPPM REST API — Activities
  description: 'Oracle Primavera P6 EPPM REST API — Activities. 4 operations. Lead operation: List activities. Self-contained
    Naftiko capability covering one Oracle Primavera business surface.'
  tags:
  - Oracle Primavera
  - Activities
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ORACLE_PRIMAVERA_API_KEY: ORACLE_PRIMAVERA_API_KEY
capability:
  consumes:
  - type: http
    namespace: p6-eppm-activities
    baseUri: https://{host}/p6ws/rest/v1
    description: Oracle Primavera P6 EPPM REST API — Activities business capability. Self-contained, no shared references.
    resources:
    - name: activities
      path: /activities
      operations:
      - name: listactivities
        method: GET
        description: List activities
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Fields
          in: query
          type: string
        - name: Filter
          in: query
          type: string
          description: Filter by project or WBS; e.g. ProjectObjectId eq 12345
        - name: offset
          in: query
          type: integer
        - name: limit
          in: query
          type: integer
      - name: createactivity
        method: POST
        description: Create an activity
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: activities-objectId
      path: /activities/{objectId}
      operations:
      - name: getactivity
        method: GET
        description: Get an activity
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Fields
          in: query
          type: string
      - name: updateactivity
        method: PUT
        description: Update an activity
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.ORACLE_PRIMAVERA_API_KEY}}'
  exposes:
  - type: rest
    namespace: p6-eppm-activities-rest
    port: 8080
    description: REST adapter for Oracle Primavera P6 EPPM REST API — Activities. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/activities
      name: activities
      description: REST surface for activities.
      operations:
      - method: GET
        name: listactivities
        description: List activities
        call: p6-eppm-activities.listactivities
        with:
          Fields: rest.Fields
          Filter: rest.Filter
          offset: rest.offset
          limit: rest.limit
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createactivity
        description: Create an activity
        call: p6-eppm-activities.createactivity
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/activities/{objectid}
      name: activities-objectid
      description: REST surface for activities-objectId.
      operations:
      - method: GET
        name: getactivity
        description: Get an activity
        call: p6-eppm-activities.getactivity
        with:
          Fields: rest.Fields
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updateactivity
        description: Update an activity
        call: p6-eppm-activities.updateactivity
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: p6-eppm-activities-mcp
    port: 9090
    transport: http
    description: MCP adapter for Oracle Primavera P6 EPPM REST API — Activities. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: list-activities
      description: List activities
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: p6-eppm-activities.listactivities
      with:
        Fields: tools.Fields
        Filter: tools.Filter
        offset: tools.offset
        limit: tools.limit
      outputParameters:
      - type: object
        mapping: $.
    - name: create-activity
      description: Create an activity
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: p6-eppm-activities.createactivity
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: get-activity
      description: Get an activity
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: p6-eppm-activities.getactivity
      with:
        Fields: tools.Fields
      outputParameters:
      - type: object
        mapping: $.
    - name: update-activity
      description: Update an activity
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: p6-eppm-activities.updateactivity
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
